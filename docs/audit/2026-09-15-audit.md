# Audit record — 2026-09-15

The independent audit that produced this restructure programme. Filed
2026-09-15 against `main` @ `cd9a03b`.

## Method

Three independent read-only auditors, run in parallel:

1. **Code correctness** — the instrument and gate against the
   definitions and rules they claim to implement
   (`theory/CRR.md` §1–§5; CLAUDE.md R4–R6).
2. **Docs/protocol coherence** — whether the tree, the protocol document
   and the theory document agree with each other.
3. **Reproducibility (R9)** — re-running every emitting script and
   comparing outputs byte-for-byte.

Plus a **full-chain smoke** of the whole pipeline at `cd9a03b`.

## Baseline receipts

Verified at `cd9a03b`; every PR must leave them true:

- `uv sync --frozen` + `uv lock --check` OK;
  `sha256(uv.lock) = 052566c3725e02d85ee30cb49d24719dee5201d2c4a8f626967d16ae1534a10c`
- `uv run pytest instrument/tests -q` → 9 passed
- `theory/checks/verify_math.py` + `theory/checks/verify_scope_math.py`
  → all PASS
- Gates L5 / CUT / T1 → **GATE OPEN**, with the expected control
  verdicts: S-H FAIL / S-H2 PASS on T1; FM sine + S-G2 PASS on L5;
  S-E PASS on CUT
- Reproducibility audit: every emitting script byte-identical across two
  runs (`cmp`)

## Findings

As filed 2026-09-15 (issue map). Dispositions name the PR in the
restructure programme that owns each fix; merge order is
restructure → environment → docs.

| # | severity | title | disposition |
|---|---|---|---|
| #2 | high | Gate layer: 6 correctness defects vs A1′/R5 | fix in restructure PR |
| #3 | high | No tests pin gate verdicts / MUST_FAIL–MUST_PASS contracts | fix in restructure PR |
| #4 | med | Instrument gaps vs §3.1/D1 (ρ, sign test, duplicate cuts, guards) | fix in restructure PR |
| #5 | med | Battery thinner than §3.2 documents (λ/ρ sweeps, μ=1 vDP, Rössler, Duffing) | fix in restructure PR (implement) |
| #6 | med | gate_T1 omits lr control required by T1x-1 | fix in restructure PR (report lr-controlled columns) |
| #7 | high | .gitignore excludes runs/ stdout logs required by R1/§7/§8 | fix in restructure PR |
| #8 | high | CLAUDE.md vs tree documentation debt | fix in restructure PR |
| #9 | med | SCOPE.md gate numbers with no committed emitting output | fix in restructure PR (commit `runs/phaseA/`) |
| #10 | med | PREREG_TEMPLATE omits R4/R6 fields | fix in restructure PR |
| #11 | med | Environment half-pinned (interpreter, provenance header) | `.python-version` + CI in environment PR; provenance header in scripts/ |
| #12 | med | Implement gate_EQ + replay-learner surrogates (R4 blocker) | owner/subsequent work — not in restructure PR |
| #13 | low | Theory v3.1 decisions pending human approval (A8′, H-T1 wording) | owner decision — no PR may apply |
| #14 | — | Restructure programme umbrella | closed by the three PRs |

The committed gate tables under `runs/phaseA/` are the emitting output
for the gate statistics quoted elsewhere (finding #9's remedy); the
ledger remains empty — no hypothesis has a held-out PASS.

No numbers beyond the receipts above appear in this record.
